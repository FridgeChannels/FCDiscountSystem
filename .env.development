# Local demo helpers (Vite). Safe to commit overrides in .env.local instead.
VITE_DEMO_FORCE_RENEW_ENABLED=true

# Dashboard (experience + reorder APIs). Currently listening on 8081.
FC_REORDER_API_PROXY_TARGET=http://localhost:8081
FC_DASHBOARD_PROXY_TARGET=http://localhost:8081
